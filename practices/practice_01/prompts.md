# Журнал запросов и проверок

Не сохраняйте скрытую Chain of Thought и полный чат. Нужны запрос, краткий результат, ссылка на изменённый артефакт и ваша проверка.

| ID | Артефакт и цель | Инструмент / модель | Тип промпта | Запрос или ссылка на него | Результат или ссылка | Что приняли | Что отклонили или исправили | Как проверили |
|---|---|---|---|---|---|---|---|---|
| P1-01 | Baseline-ревью `TRAINING_PR.diff` | GPT-5 | zero-shot | Посмотри practices/practice_01/TRAINING_PR.diff и найти проблемы | I will open the diff file to review its contents and identify issues.

I will quickly scan the repository to see the surrounding code for `app/review_service.py`, `app/api.py`, and `app/dependencies` to provide a precise review with context.

✱ Glob "app/**/*.py" 0 matches
 
Ниже — замечания по диффу, отсортированы по серьёзности, с привязкой к файлам и строкам из TRAINING_PR.diff.

Критичные
- app/api.py:35-38. Нет схемы тела запроса. Параметр `payload: dict` не валидируется, при отсутствии ключа `"diff"` будет `KeyError` и 500 вместо 422. Нужна Pydantic‑модель для входа.
- app/review_service.py:19-22. Нет ограничений на размер `diff`. Большие диффы вздуют промпт, вызовут ошибки/таймауты LLM, непредсказуемые счета и деградацию производительности. Нужны лимит/усечение.
- app/review_service.py:19-22. Риск prompt injection. Пользовательский `diff` напрямую вкладывается в промпт без системных инструкций/сегрегации. Нужны строгие инструкции и безопасная вставка (маркерные блоки, экранирование).

Высокой важности
- app/api.py:35-38. Нет обработки ошибок вокруг `review_service.review(...)`. Исключения из LLM будут 500. Добавить перехват и маппинг ошибок на корректные HTTP‑коды.
- app/api.py:35-38. Семантика эндпоинта “create_review” как POST предполагает создание ресурса, но возвращается просто результат без id/места хранения. Либо явно сделать вычислительный эндпоинт (например, POST /api/reviews: 200 OK), либо возвращать 201 и дескриптор созданного ресурса, если он создаётся.

Средней важности
- app/api.py:35-38. Возвращаемый тип/схема не описаны явно (нет response‑модели), OpenAPI будет менее точным. Лучше Pydantic‑модель ответа.
- app/review_service.py:19-22. Сервис возвращает `dict[str, str]`, что подмешивает HTTP‑формат на уровень домена. Логичнее, чтобы сервис возвращал просто строку (отзыв), а API формировал JSON.
- app/api.py:35-38. Синхронный эндпоинт с потенциально долгим I/O (вызов LLM) забивает воркеры. Рассмотреть `async` + асинхронный клиент/очереди/фоновые задачи, или явный executor/таймауты.
- Совместимость с версией Python. Использование `dict[str, str]` и `Protocol` требует 3.9+ и 3.8+ соответственно. Если проект целится ниже, нужны `typing.Dict` и `typing_extensions.Protocol`.

Низкой важности
- app/review_service.py:10-13. Изменение объявления метода протокола на многострочное с `...` — стилистический шум без пользы.
- Формулировка

Ниже — замечания по диффу TRAINING_PR.diff, отсортированы по серьёзности с привязкой к строкам диффа.

Критичные
- app/api.py (35–38): Отсутствует схема тела запроса. `payload: dict` не валидируется; при отсутствии ключа `"diff"` будет `KeyError` и 500 вместо 422. Нужна Pydantic‑модель запроса с обязательным полем `diff`.
- app/review_service.py (19–22): Неограниченный размер входного `diff`. Большие диффы раздуют промпт, вызовут таймауты/ошибки LLM и деградацию производительности. Нужны лимиты/усечение и, по возможности, чанкинг.
- app/review_service.py (19–22): Prompt injection. Пользовательский `diff` напрямую вставляется в промпт без системных инструкций и без явного разграничения контекста. Нужен системный блок, чёткие инструкции, явное отделение диффа (например, в тройных кавычках с маркерами), и фильтрация.

Высокой важности
- app/api.py (35–38): Нет обработки исключений от `review_service.review(...)`. Любая ошибка LLM приведёт к 500. Нужен перехват и маппинг на корректные HTTP‑коды (422/400 для валидации, 503/502 для внешних зависимостей, 408/504 для таймаутов).
- app/api.py (35–38): Семантика ресурса. POST на `/api/reviews` по идее создаёт ресурс, но возвращается только вычисленный результат без id/ссылки. Нужно либо оформить как вычислительный эндпоинт (и документировать это), либо реально создавать ресурс/запись и возвращать 201 + дескриптор.
- app/review_service.py (19–22): Неструктурированный результат. Возвращается `{"comment": answer}`, где `answer` непредсказуем по форме. Для автоматической проверки и UX лучше требовать от LLM структурированный вывод (список проблем с severities, файлами, строками) и валидировать/парсить.

Средней важности
- app/api.py (35–38): Нет response‑модели, OpenAPI будет размытым. Лучше определить Pydantic‑схему ответа.
- app/review_service.py (19–22): Сервис доменного уровня возвращает JSON‑словарь. Лучше отделить доменную логику (возвращать модель/строку) от представления (формировать JSON на уровне API).
- app/api.py (35–38): Синхронный обработчик с потенциально долгим I/O (LLM) блокирует воркеры. Рассмотреть `async` и асинхронный клиент/очередь с фоновой обработкой, таймауты, или явный executor.
- app/review_service.py (19–22): Отсутствие таймаутов/ретраев для `llm.generate`. Любая деградация внешнего сервиса подвесит запрос.
- Совместимость типов: Использование `dict[str, str]` и `Protocol` требует Python 3.9+ и 3.8+ соответственно. Если целевая версия ниже, потребуется `typing.Dict` и `typing_extensions.Protocol`.

Низкой важности
- app/review_service.py (10–13): Изменение сигнатуры метода протокола на многострочную с `...` — чисто стилистический шум без пользы.
- app/api.py (35–38): Нет логирования запросов/исключений — усложнит диагностику.
- Безопасность/квоты: Нет контроля доступа, rate limiting или квот на вызовы LLM — риск злоупотребления.

Открытые вопросы
- Какая ожидаемая структура результата ревью? Нужны ли уровни серьёзности, привязки к файлам/строкам, ссылки на правила?
- Какая версия Python целевая для сервиса?
- Требуется ли аутентификация/авторизация для этого эндпоинта?
- Какие ограничения по размеру/весу диффа допустимы? Нужны ли чанки/постраничная обработка?

Краткое резюме изменений
- В API добавить Pydantic‑модели запроса/ответа, валидацию, обработку ошибок, возможную асинхронность и/или фоновые задачи, а также документацию по семантике ресурса.
- В сервисе LLM ограничить размер diff, добавить системные инструкции и безопасную вставку контента (разделители, экранирование), запросить структурированный вывод, добавить таймауты/ретраи.
- По возможности разделить доменную логику и слой представления, и добавить базовое логирование. |  |  |  |
| P1-02 | Повторное ревью с master prompt | GPT-5 | master prompt | См. Master Prompt v1 ниже | Summary: добавлен POST `/api/reviews` без валидации входа; сервис `ReviewService.review` формирует промпт с полным diff и возвращает `{"comment": answer}`. Риски: 1) SEC-1 — нет редактирования секретов перед LLM; 2) API-1 — нет ограничения длины diff/413; 3) OUT-1 — формат ответа не соответствует контракту. Checks: тест 413 при длинном diff; тест редактирования секретов; тест структуры ответа. | Приняли риски с evidence, привязанные к строкам диффа и правилам | Отклонили предположения вне diff (аутентификация, rate limiting) | Проверили против TRAINING_PR.diff и правил из CASE.md |
| P1-03 |  |  |  |  |  |  |  |  |

## Master Prompt v1

Соберите здесь контракт второго запуска. Не копируйте все документы целиком — ставьте ссылки на файлы и переносите только необходимый для задачи контекст.

### 1. Цель и роль

- Цель: провести ревью учебного PR по diff и вернуть summary, ≤3 риска с доказательствами и список проверок.
- Роль AI: AI‑reviewer, который опирается только на diff и правила (SEC‑1…QA‑1), не выполняет действий в репозитории.

### 2. Входы и источники

- Обязательный вход: practices/practice_01/TRAINING_PR.diff.
- Разрешённые файлы и источники: только diff и CASE.md для правил.
- Context Pack — факты, правила, примеры и ограничения: см. practices/practice_01/context.md (SEC‑1, API‑1, OUT‑1, QA‑1, SCOPE‑1).

### 3. Задача и артефакты

- Что сделать: прочитать diff, выявить подтверждённые риски, сформировать проверяемые evidence и предложить проверки.
- Что вернуть: JSON с `summary`, `risks` (≤3), `checks`.

### 4. Формат результата

- Структура ответа: { summary: string, risks: [{ file, line, evidence, risk }], checks: string[] }.
- Ограничения объёма: не более 3 рисков; без лишних рассуждений.

### 5. Полномочия и запреты

- Разрешено: анализировать diff, ссылаться на правила.
- Запрещено: approve, merge, редактировать код; придумывать правила.

### 6. Рабочий процесс и остановка

- Шаги: 1) Прочитать diff; 2) Сопоставить с правилами; 3) Сформировать summary; 4) Отобрать ≤3 риска с evidence; 5) Составить checks. 6) Вернуть JSON.
- Когда остановиться и запросить человека: если риск не подтверждён diff или правилом — не включать его (QA‑1) и завершить.

### 7. Проверки и evidence

- Как проверять утверждения: все риски подтверждать строками из diff или ссылкой на правило.
- Какое evidence сохранить: точные цитаты из diff (строки/фрагменты) и указание файла и строк по TRAINING_PR.diff.

### 8. Definition of Done

- Задача закончена, когда: возвращены summary, ≤3 риска с file:line и evidence, и список checks; каждый риск подтверждён diff или правилом; нет запрещённых действий.

## Сравнение двух запусков

| Проверка | Zero-shot | С master prompt | Вывод команды |
|---|---|---|---|
| Есть ссылка на файл или строку |  |  |  |
| Вывод подтверждён diff или правилом |  |  |  |
| Соблюдены границы AI |  |  |  |
| Есть воспроизводимая проверка |  |  |  |

## Peer review

| Где другой команде пришлось догадываться | Что исправили | Если не исправили — почему |
|---|---|---|
| 1 |  |  |
| 2 |  |  |
| 3 |  |  |
Артефакты: practices/practice_01/TRAINING_PR.diff, practices/practice_01/CASE.md, practices/practice_01/context.md

Возврат строго в формате JSON.

## AI-reviewer Output v1

```json
{
  "summary": "Добавлен POST /api/reviews, который вызывает ReviewService.review. ReviewService формирует промпт с полным diff и возвращает {\"comment\": answer}. В коде нет редактирования секретов, нет ограничения длины diff/ответа 413, и формат ответа не соответствует OUT-1.",
  "risks": [
    {
      "file": "app/review_service.py",
      "line": "19-22",
      "candidate": "SEC-1: diff отправляется во внешний LLM без удаления секретов",
      "evidence": "prompt = f\"Review this pull request and find problems:\n{diff}\"\nanswer = self.llm.generate(prompt)",
      "rule": "SEC-1",
      "check": "Юнит-тест: передать в review diff со строкой 'token=abc123'; LLM замокать и зафиксировать prompt; убедиться, что вместо значения секрета в prompt '[REDACTED]'"
    },
    {
      "file": "app/api.py",
      "line": "35-38",
      "candidate": "API-1: нет ограничения длины diff и отказа 413",
      "evidence": "@app.post(\"/api/reviews\")\ndef create_review(payload: dict) -> dict[str, str]:\n    return review_service.review(payload[\"diff\"])",
      "rule": "API-1",
      "check": "Интеграционный тест: POST /api/reviews с diff длиной > 20000 символов должен вернуть 413"
    },
    {
      "file": "app/review_service.py",
      "line": "21-22",
      "candidate": "OUT-1: формат ответа не соответствует требуемой структуре (summary/risks/checks)",
      "evidence": "return {\"comment\": answer}",
      "rule": "OUT-1",
      "check": "Интеграционный тест: POST /api/reviews возвращает JSON со свойствами summary, risks (≤3) и checks"
    }
  ],
  "checks": [
    "POST /api/reviews с diff > 20000 символов -> 413",
    "Юнит-тест ReviewService: редактирование секретов в prompt по SEC-1",
    "Контракт ответа по OUT-1: summary/risks/checks"
  ]
}
```
