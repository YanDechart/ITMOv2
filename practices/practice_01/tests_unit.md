# Unit-проверки

| Требование или правило | Что проверяем изолированно | Вход | Ожидаемый результат | Evidence |
|---|---|---|---|---|
| SEC-1 | Редакция секретов перед отправкой в LLM | diff с `token=abc123` и `password=pass42` | В сформированном prompt значения секретов заменены на `[REDACTED]` | Строки prompt не содержат исходных значений секретов |
| OUT-1 | Формирование структурированного ответа | мок ответа LLM и маппер | Возвращается структура `{summary, risks(≤3), checks}` | Парсер/валидатор возвращает валидную модель |
| QA-1 | Отбор рисков только при подтверждении | мок: 4 риска — 2 без evidence; 1 с evidence, но он не подтверждён строкой diff/правилом; 1 с валидным evidence, подтверждённым строкой diff или правилом CASE.md | В итоговом списке остаётся только подтверждённый риск (и общее количество ≤3); элементы без evidence и с неподтверждённым evidence исключены; каждый оставшийся риск имеет `file`, `line`, `evidence`, `risk` | Фильтр исключает элементы без evidence и элементы, evidence которых не подтверждён строкой diff или правилом CASE.md; подтверждённый риск остаётся |

## Как использовали AI

- Для чего: вывести минимальные unit-проверки по SEC‑1, OUT‑1 и усиленному QA‑1 (отсев неподтверждённых рисков).
- Тип промпта: master prompt P1‑02 (контракт ревью и проверки).
- Строка в [`prompts.md`](prompts.md): P1-02.
- Что проверили и исправили сами: добавили проверку для QA‑1 на случай, когда evidence присутствует, но не подтверждается diff/правилом; не вводили требование точного совпадения строк, достаточно цитаты/ссылки по CASE.md.
